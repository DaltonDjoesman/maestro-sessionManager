import { pt } from "./pt";

/**
 * English locale stub — same shape as `pt`.
 * Falls back to pt-PT copy until strings are translated; add EN overrides incrementally.
 */
export const en: typeof pt = pt;
